# Standby — Session 14 Follow-Up

## Post-F8D / Pre-GI Gas and Coverage Baseline

F8D implementation is complete and:

> **G8D — PASS / CLOSED**

Before updating docs/project-status.md and before beginning GI, capture the first repository-wide engineering baseline for:

1. gas usage;
2. test coverage.

This is a **measurement and evidence task only**.

F0–F8D production path complete
G8D PASS / CLOSED
↓
POST-F8D / PRE-GI BASELINE
↓
project-status update  
 ↓
session 14 closout
↓
GI not started

---

# 1. Authority Boundary

Apply the established clean rule:

> **`CLAUDE.md` owns permanent operating behavior.**
>
> **`.claude/rules/*` owns permanent Solidity/testing conventions.**
>
> **This Session 14 follow-up owns only this evidence task's objective, scope, requirements, prohibitions, file boundaries, evidence requirements, and completion boundary.**

---

# 2. Objective

Capture the repository state at the following checkpoint:

```text id="lhbf6w"
F0–F8D production path complete
G8D PASS / CLOSED
        ↓
POST-F8D / PRE-GI BASELINE
        ↓
GI not started
```

Create:

```text id="2wfr02"
docs/reports/gas-snapshot.md
docs/reports/coverage-summary.md
```

Preserve the normal machine-readable gas snapshot generated by Foundry, if applicable.

These reports are:

> **Non-normative engineering evidence.**

They do not define protocol semantics and do not independently establish or close an implementation gate.

---

# 3. Evidence Context

Both reports must identify the measured repository state sufficiently to make the baseline reproducible.

Record:

- measurement date;
- Git branch;
- Git commit SHA;
- working-tree state relevant to this measurement;
- Forge / Foundry version;
- active Foundry profile where relevant;
- existing local Foundry gas-limit configuration where relevant;
- exact measurement command.

If the current Session 14 work is not yet committed, state that explicitly.

Do not represent the measurement as belonging to a commit that does not contain the measured state.

---

# 4. Gas Baseline

Run:

```bash id="w0eiyz"
forge snapshot
```

unless an existing permanent repository convention requires a different invocation.

Do not change gas-related configuration for this task.

Preserve the normal machine-comparable gas snapshot artifact produced by Foundry where applicable.

Create:

```text id="6y3j6r"
docs/reports/gas-snapshot.md
```

The report must contain:

## Status

State:

> **Status: Non-normative engineering evidence. This report does not define protocol semantics or independently establish a verification gate.**

## Checkpoint

Identify the measurement as:

```text id="7e78ha"
Post-F8D / Pre-GI
```

## Environment

Record the evidence context from Section 3.

## Command and Result

Record:

- exact command executed;
- whether it completed successfully;
- any material limitation affecting interpretation.

## Measurements

Summarize the materially useful gas measurements reported for the Standby production implementation.

Report actual measured values rather than generalized assessments.

Preserve the machine-readable snapshot as the detailed comparison source where applicable.

## Baseline Purpose

State that this report establishes the post-F8D / pre-GI gas baseline against which later measurements may be compared.

## Observations

Record any materially noteworthy measurement observation.

Do not turn an observation into a new optimization requirement.

If there is no material observation, state that.

---

# 5. Coverage Baseline

Run:

```bash id="e6vq8w"
forge coverage
```

unless an existing permanent repository convention requires a different invocation.

Create:

```text id="q4d4mm"
docs/reports/coverage-summary.md
```

The report must contain:

## Status

State:

> **Status: Non-normative engineering evidence. This report does not define protocol semantics or independently establish a verification gate.**

## Checkpoint

Identify the measurement as:

```text id="66pw0s"
Post-F8D / Pre-GI
```

## Environment

Record the evidence context from Section 3.

## Command and Result

Record:

- exact command executed;
- whether it completed successfully;
- any reporting options used;
- any material limitation affecting interpretation.

## Repository-Level Coverage

Record the distinct coverage dimensions reported by Foundry, where available:

- line coverage;
- statement coverage;
- branch coverage;
- function coverage.

Do not collapse the distinct dimensions into one generalized coverage percentage.

## Production Coverage

Where reliable per-file results are available, summarize coverage for the relevant Standby production contracts.

Use the actual repository production-contract set.

## Material Uncovered Production Areas

Identify materially uncovered or partially covered production paths that may deserve examination when GI is derived.

Do not classify an uncovered line or branch as a protocol defect solely because it is uncovered.

## Potential GI Inputs

Identify any uncovered production behavior that intersects an invariant-relevant or state-transition-relevant path and may therefore warrant examination during GI.

Record these as **potential GI inputs**, not as already-established GI requirements.

Do not begin GI derivation or implementation.

---

# 6. Interpretation Boundary

Preserve the following distinction:

```text id="ew04gl"
Gas measurement
    ≠ gas optimization requirement

Coverage measurement
    ≠ semantic correctness

High coverage
    ≠ invariant proof

Uncovered code
    ≠ automatic protocol defect
```

The reports expose implementation characteristics for subsequent engineering review.

They do not alter the frozen protocol semantics or the already-closed implementation gates.

---

# 7. Scope / Prohibitions

This task authorizes measurement and evidence capture only.

Do not:

- modify Solidity production code;
- modify tests;
- modify harnesses;
- optimize gas;
- add tests to improve coverage;
- remove or suppress tests to affect coverage;
- exclude production code merely to improve reported coverage;
- change gas configuration;
- change compiler, optimizer, or EVM configuration;
- change dependencies;
- modify frozen normative artifacts;
- modify `docs/project-status.md`;
- reopen or redesign F8D;
- begin GI;
- begin F9 or F10;
- perform unrelated cleanup or refactoring.

If either baseline cannot be captured without making one of these changes, do not make the change.

Record the failed command and blocker instead.

---

# 8. Authorized Files

This follow-up may create or update only:

```text id="48h55a"
docs/reports/gas-snapshot.md
docs/reports/coverage-summary.md
docs/prompts/session-14-log.md
```

and the normal machine-readable gas snapshot artifact produced by `forge snapshot`, if applicable.

No other repository file modification is authorized.

---

# 9. Session 14 Log

Update:

```text id="4jxd3m"
docs/prompts/session-14-log.md
```

with a concise record that the post-F8D / pre-GI baseline was captured.

Record:

- gas command and result;
- coverage command and result;
- reports created;
- machine-readable gas snapshot created or updated, if applicable;
- material measurement limitations or anomalies;
- confirmation that no production code, tests, configuration, or downstream implementation work was performed.

Do not duplicate the detailed report contents into the session log.

Reference the two detailed evidence artifacts:

```text id="b03x95"
docs/reports/gas-snapshot.md
docs/reports/coverage-summary.md
```

---

# 10. Completion Report

When complete, report:

1. files created;
2. files modified;
3. exact gas command and result;
4. exact coverage command and result;
5. headline gas measurements;
6. repository-level coverage results;
7. materially uncovered production areas, if any;
8. potential GI inputs identified, if any;
9. measurement limitations or blockers;
10. confirmation that no production code, tests, harnesses, or configuration were changed;
11. confirmation that GI was not started.

Do not perform corrective implementation work based on the measurements.

---

# 11. Completion Boundary

The completion boundary is:

```text id="grr9fl"
post-F8D / pre-GI gas baseline captured
docs/reports/gas-snapshot.md created
machine-readable gas snapshot preserved where applicable
post-F8D / pre-GI coverage baseline captured
docs/reports/coverage-summary.md created
session-14-log.md updated
```

Stop at the post-F8D / pre-GI engineering-baseline completion boundary.
